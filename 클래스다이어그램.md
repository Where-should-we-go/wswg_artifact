# 클래스 다이어그램 — 어디갈래?(WSWG)

> Spring Boot 백엔드 소스(`com.ssafy.wswg`, 126 클래스) 기반으로 작성.
> 아키텍처: **Controller → Service → DAO(MyBatis) → PostgreSQL**, 외부 연동(TourAPI · OpenAI · Naver · ODsay), 실시간 협업(WebSocket + Redis Stream).

---

## 1. 도메인 모델 (핵심 엔티티 · DTO)

서비스가 다루는 핵심 데이터 구조와 관계. `trips.data`는 블록 에디터 JSON(JSONB)으로 일정·미디어를 담는다.

```mermaid
classDiagram
    direction LR

    class UserDto {
        +Long id
        +String name
        +String email
        +String profileImageUrl
        +Role role
        +LocalDate createdAt
    }

    class GroupDto {
        +Long groupId
        +String groupName
        +Long ownerId
        +OffsetDateTime createdAt
        +OffsetDateTime joinedAt
        +Integer memberCount
        +Integer tripCount
    }

    class TripDto {
        +Long tripId
        +String title
        +LocalDate startDate
        +LocalDate endDate
        +Long userId
        +Long groupId
        +String groupName
        +JsonNode data
        +OffsetDateTime createdAt
        +OffsetDateTime updatedAt
    }

    class AttractionDto {
        +Integer contentId
        +String title
        +Integer contentTypeId
        +Integer sidoCode
        +Integer gugunCode
        +Double latitude
        +Double longitude
        +String addr1
        +String overview
        +String firstImage1
    }

    class GroupMediaDto {
        +Long tripId
        +String blockId
        +Integer contentId
        +String attractionTitle
        +Double latitude
        +Double longitude
        +String mediaType
        +String mediaUrl
        +Boolean representative
        +JsonNode metadata
    }

    class SidoDto {
        +Integer sidoCode
        +String sidoName
    }
    class GugunDto {
        +Integer sidoCode
        +Integer gugunCode
        +String gugunName
    }
    class ContentTypeDto {
        +Integer contentTypeId
        +String name
    }

    class Role {
        <<enumeration>>
        USER
        ADMIN
    }
    class TripStatus {
        <<enumeration>>
        UPCOMING
        ONGOING
        COMPLETED
    }

    UserDto "1" --> "0..*" GroupDto : owns(ownerId)
    GroupDto "0..*" --> "0..*" UserDto : members(user_group)
    UserDto "1" --> "0..*" TripDto : creates
    GroupDto "1" --> "0..*" TripDto : has
    TripDto "1" --> "0..*" GroupMediaDto : derives(trips.data)
    GroupMediaDto "0..*" --> "1" AttractionDto : refers(contentId)
    AttractionDto "0..*" --> "1" SidoDto : in
    AttractionDto "0..*" --> "1" GugunDto : in
    AttractionDto "0..*" --> "1" ContentTypeDto : typed
    SidoDto "1" --> "0..*" GugunDto : contains
    UserDto ..> Role
    TripDto ..> TripStatus
```

---

## 2. 계층형 아키텍처 (전체 흐름)

요청이 Controller → Service → DAO로 흐르고, 일부 Service는 외부 클라이언트를 호출한다.

```mermaid
classDiagram
    direction TB

    class AuthController
    class AttractionController
    class RegionController
    class TripController
    class GroupController
    class MyPageController
    class AiTripRecommendationController
    class AdminTourController

    class AttractionService
    class AttractionSearchService
    class AttractionDetailService
    class RegionService
    class TripService
    class GroupService
    class TripMediaService
    class AiTripPlanService
    class AiTripRecommendationService
    class RoutingService
    class TourLoadService
    class RefreshTokenService

    class UserDao
    class AttractionDao
    class RegionDao
    class ContentTypeDao
    class TripDao
    class GroupDao
    class BatchRunLogDao

    class TourApiClient
    class OpenAiEmbeddingClient
    class OpenAiTripCandidateClient
    class NaverDirectionsClient
    class OdsayTransitClient

    AuthController --> RefreshTokenService
    AuthController --> UserDao
    AttractionController --> AttractionSearchService
    AttractionController --> AttractionDetailService
    AttractionController --> AttractionService
    RegionController --> RegionService
    TripController --> TripService
    TripController --> TripMediaService
    GroupController --> GroupService
    MyPageController --> TripService
    MyPageController --> GroupService
    AiTripRecommendationController --> AiTripRecommendationService
    AiTripRecommendationController --> AiTripPlanService
    AiTripRecommendationController --> RoutingService
    AdminTourController --> TourLoadService

    AttractionSearchService --> AttractionDao
    AttractionDetailService --> AttractionDao
    AttractionDetailService --> TourApiClient
    RegionService --> RegionDao
    RegionService --> ContentTypeDao
    TripService --> TripDao
    GroupService --> GroupDao
    AiTripRecommendationService --> AttractionDao
    AiTripRecommendationService --> OpenAiEmbeddingClient
    AiTripPlanService --> OpenAiTripCandidateClient
    AiTripPlanService --> TripDao
    RoutingService --> NaverDirectionsClient
    RoutingService --> OdsayTransitClient
    TourLoadService --> TourApiClient
    TourLoadService --> AttractionDao
    TourLoadService --> BatchRunLogDao
```

---

## 3. 여행·모임 도메인 상세 (메서드 시그니처)

```mermaid
classDiagram
    direction LR

    class TripController {
        +createTrip(TripCreateRequestDto) TripDto
        +readMyTrips() List~TripDto~
        +readTrip(tripId) TripDto
        +uploadMedia(tripId, file) TripMediaUploadResponse
        +updateTrip(tripId, TripUpdateRequestDto) TripDto
        +deleteTrip(tripId) void
    }
    class TripService {
        +createTrip(userId, TripCreateRequestDto) TripDto
        +readTrip(tripId, userId) TripDto
        +readMyTrips(userId) List~TripDto~
        +readGroupTrips(groupId, userId) List~TripDto~
        +readMyPageTrips(userId, scope, status) List~MyPageTripResponse~
        +updateTrip(tripId, userId, TripUpdateRequestDto) TripDto
        +deleteTrip(tripId, userId) void
    }
    class TripDao {
        <<interface>>
        +createTrip(TripDto) int
        +readTripById(tripId) TripDto
        +readTripsByUserId(userId) List~TripDto~
        +updateTrip(TripDto) int
        +deleteTrip(tripId) int
    }

    class GroupController {
        +createGroup(GroupCreateRequestDto) GroupDto
        +readGroups() List~GroupDto~
        +readGroup(groupId) GroupDto
        +readMembers(groupId) List~GroupMemberDto~
        +readFootprint(groupId) List~GroupFootprintDto~
        +readMedia(groupId) List~GroupMediaDto~
        +createInviteLink(groupId) GroupInviteLinkDto
        +joinGroup(GroupJoinRequestDto) GroupJoinRequestStatusDto
        +approveJoinRequest(groupId, requestId) void
    }
    class GroupService {
        +createGroup(userId, GroupCreateRequestDto) GroupDto
        +readGroups(userId) List~GroupDto~
        +readMembers(groupId, userId) List~GroupMemberDto~
        +readFootprints(groupId, userId) List~GroupFootprintDto~
        +readMedia(groupId, userId, GroupMediaRequest) List~GroupMediaDto~
        +addMember(groupId, userId, GroupMemberAddRequestDto) GroupMemberDto
        +createInviteLink(groupId, userId) GroupInviteLinkDto
        +joinGroup(userId, GroupJoinRequestDto) GroupJoinRequestStatusDto
    }
    class GroupDao {
        <<interface>>
        +createGroup(GroupDto) int
        +readGroupsByUserId(userId) List~GroupDto~
        +readJoinRequest(...) GroupJoinRequestStatusDto
        +deleteGroup(groupId) int
    }

    TripController --> TripService
    TripService --> TripDao
    GroupController --> GroupService
    GroupService --> GroupDao
```

---

## 4. AI 여행 자동 생성 · 추천 (외부 연동)

3단계 흐름: ① 후보 생성(GPT) → ② 의미 검색 매칭(임베딩 + pgvector) → ③ 이동거리·플랜 확정.

```mermaid
classDiagram
    direction LR

    class AiTripRecommendationController {
        +tripCandidates(AiTripCandidateRequest) AiTripCandidateResponse
        +tripRecommendations(AiTripRecommendationRequest) AiTripRecommendationResponse
        +travelLegs(TravelLegsRequest) List~RouteLeg~
        +tripPlans(AiTripPlanCreateRequest) TripDto
    }
    class AiTripRecommendationService {
        +recommend(AiTripRecommendationRequest) AiTripRecommendationResponse
    }
    class AiTripPlanService {
        +createPlan(userId, AiTripPlanCreateRequest) TripDto
    }
    class RoutingService {
        +legs(TravelLegsRequest) List~RouteLeg~
    }

    class OpenAiEmbeddingClient {
        +embed(text) float[]
    }
    class OpenAiTripCandidateClient {
        +generate(prompt) AiTripCandidateDto
    }
    class NaverDirectionsClient {
        +route(from, to) RouteLeg
    }
    class OdsayTransitClient {
        +transit(from, to) RouteLeg
    }
    class AttractionDao {
        <<interface>>
        +findNearbyAttractions(...) List~NearbyAttractionDto~
        +findSemanticNearbyAttractions(...) List~SemanticAttractionDto~
        +findAiTripRecommendationMatches(...) List~AiTripRecommendationMatchDto~
    }

    AiTripRecommendationController --> AiTripRecommendationService
    AiTripRecommendationController --> AiTripPlanService
    AiTripRecommendationController --> RoutingService
    AiTripRecommendationService --> OpenAiEmbeddingClient
    AiTripRecommendationService --> AttractionDao
    AiTripPlanService --> OpenAiTripCandidateClient
    RoutingService --> NaverDirectionsClient
    RoutingService --> OdsayTransitClient
    RoutingService ..> TravelMode
```

---

## 5. 실시간 공동 편집 (WebSocket + Redis Stream)

여러 사용자가 같은 여행 일정을 동시 편집한다. 편집 이벤트는 Redis Stream으로 브로드캐스트되고, 주기적으로 `trips.data`에 플러시된다.

```mermaid
classDiagram
    direction TB

    class PlanEditWebSocketHandler {
        +afterConnectionEstablished(session)
        +handleTextMessage(session, message)
        +afterConnectionClosed(session)
    }
    class PlanEditSessionRegistry {
        +register(tripId, session)
        +remove(session)
        +sessionsOf(tripId)
    }
    class PlanStateService {
        +loadState(tripId, userId) JsonNode
        +applyEdit(tripId, userId, PlanEditEvent) PlanSocketMessage
        +syncMessage(tripId, data) PlanSocketMessage
        +appendBlockMedia(tripId, userId, itemId, media) JsonNode
        +clearPresence(tripId, userId)
        +hasState(tripId) boolean
    }
    class PlanEditRedisSubscriber {
        +onMessage(stream entry)
    }
    class PlanStateFlushWorker {
        +flush()  // scheduled
    }

    class PlanEditEvent
    class PlanSocketMessage
    class TripDao {
        <<interface>>
    }

    PlanEditWebSocketHandler --> PlanEditSessionRegistry
    PlanEditWebSocketHandler --> PlanStateService
    PlanStateService ..> PlanEditEvent
    PlanStateService ..> PlanSocketMessage
    PlanEditRedisSubscriber --> PlanEditSessionRegistry
    PlanStateFlushWorker --> PlanStateService
    PlanStateFlushWorker --> TripDao
```

---

## 6. 횡단 관심사 (보안 · 설정)

| 패키지 | 클래스(예) | 역할 |
|---|---|---|
| `security` | JWT 발급·검증 필터, 인증 토큰 처리 | 소셜 로그인 후 액세스/리프레시 토큰 기반 인증 |
| `interceptor` | 인증 인터셉터 | `@RequestMapping` 진입 시 사용자 식별(userId 주입) |
| `config` | WebSocket·Redis·MyBatis·CORS 설정 | 인프라 빈 구성 |
| `external/tour` | `TourApiClient`, `AttractionItemConverter` | TourAPI 호출·응답 변환 |
| `storage` | 파일 스토리지 | 여행 미디어(사진/음성/영상) 업로드 |
| `exception` | `GlobalExceptionHandler`, `ErrorResponseDto` | 통일된 오류 응답(502 등) |

---

### 작성 메모
- 위 다이어그램은 GitLab/GitHub Markdown에서 Mermaid로 렌더링된다. PDF·Word 제출 시 [mermaid.live](https://mermaid.live)에 붙여넣어 PNG/SVG로 내보낸 뒤 첨부한다.
- 전체 클래스 수가 많아(126) DTO·설정 클래스는 §6 표로 요약하고, **핵심 도메인·계층 흐름·시그니처** 중심으로 다이어그램을 구성했다.
